<%- include("partials/header"); -%>

  <h1>Home</h1>
  <p><%=homeContent%></p>

  <% posts.forEach(function(post){ %>
  <% let length = 100; %>
  <h1> <%= post.title %> </h1>
  <p> <%= post.post.substring(0, Math.min(length, post.post.length)) + "..." %> 
    <a href="posts/<%= _.lowerCase(post.title) %>">Read More</a>
  </p>
  <% }); %>
  
<%- include("partials/footer"); -%>